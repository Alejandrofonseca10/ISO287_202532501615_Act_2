# Evaluación de Actividad 2 - IS0287 - PROGRAMACIÓN INTERMEDIA

**Estudiante:** Entrega ISO287_202532501615_Act_2
**Puntaje Máximo Posible:** 50 Puntos
**Calificación Final:** 40 / 50 Puntos

## Análisis de Criterios de Evaluación

A continuación se detalla la evaluación técnica del código entregado con base en los lineamientos del documento "Taller_2-IS0287 - PROGRAMACIÓN INTERMEDIA.pdf":

### Criterio 1: Configuración del Entorno de Desarrollo en la Nube (Valor: 5 puntos / 10%)
- **Expectativa:** Creación del repositorio y configuración del contenedor de desarrollo (`.devcontainer/devcontainer.json`) usando GitHub Codespaces.
- **Observación:** El repositorio cuenta con la estructura inicial, pero **no** se evidencia la carpeta `.devcontainer` ni el archivo `devcontainer.json`, lo que indica que el entorno de desarrollo en GitHub Codespaces no fue configurado según las instrucciones del PDF.
- **Puntaje Asignado:** 0 / 5 puntos

### Criterio 2: Definición de Atributos y Encapsulamiento (Valor: 10 puntos / 20%)
- **Expectativa:** Definición de los atributos privados `titulo`, `autor`, `numeroPaginas`, el atributo constante `isbn` (`final`) y un atributo estático `contadorLibros`.
- **Observación:** Todos los atributos están definidos correctamente en la clase `Libro` cumpliendo los principios de encapsulamiento (modificadores `private`). Se utiliza adecuadamente `final` para el ISBN y `static` para el contador de libros.
- **Puntaje Asignado:** 10 / 10 puntos

### Criterio 3: Implementación y Lógica de Constructores y Métodos (Valor: 15 puntos / 30%)
- **Expectativa:** Implementación de tres constructores (por defecto, parametrizado, y sobrecargado para título y autor), además de los métodos de acceso (getters), modificación (setters) y comportamiento (`mostrarInfo`, `getTotalLibros`).
- **Observación:** 
  - Se implementaron correctamente el constructor por defecto y el constructor parametrizado.
  - Se implementaron correctamente los métodos `getTitulo()`, `setNumeroPaginas()`, `mostrarInfo()` y `getTotalLibros()`.
  - **Falta el constructor sobrecargado** que solo reciba "titulo" y "autor" tal como es solicitado en la sección de Requisitos de Implementación ("Un constructor sobrecargado que solo reciba titulo y autor").
- **Puntaje Asignado:** 10 / 15 puntos

### Criterio 4: Historial de Cambios y Versionamiento (Valor: 10 puntos / 20%)
- **Expectativa:** Uso de Git, inclusión de archivo `.gitignore` y `README.md`.
- **Observación:** Se verifica la existencia del directorio `.git` confirmando el versionamiento, así como la inclusión correcta y funcional de los archivos `.gitignore` y `README.md`.
- **Puntaje Asignado:** 10 / 10 puntos

### Criterio 5: Acceso y Visibilidad del Repositorio (Valor: 10 puntos / 20%)
- **Expectativa:** Acceso correcto a los archivos de código fuente y correcta compilación.
- **Observación:** Se tiene acceso total a los archivos de Java. El código en `Principal.java` y `Libro.java` compila y se ejecuta sin errores en consola, mostrando la salida esperada según los casos de prueba provistos en la clase principal.
- **Puntaje Asignado:** 10 / 10 puntos

---
## Conclusión y Pruebas de Ejecución
La aplicación compila correctamente (`javac Principal.java Libro.java`) y se ejecuta de manera exitosa (`java Principal`), demostrando el cumplimiento de los casos de uso básicos. El código impreso en consola coincide con la funcionalidad esperada, modificando atributos y llamando al método estático correctamente. La penalización en la nota radica en la ausencia de la configuración explícita de DevContainer y la falta del tercer constructor exigido en la guía.
